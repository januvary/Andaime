"""andaime.qt.dev — standalone developer tools built on andaime.

These are self-contained applications/widgets for developing andaime-based
apps (dev launcher, opencode session tree, commit assistant, project
registry). They are not shared UI components: consumer apps don't import
them, and they are not re-exported from ``andaime.qt``.
"""